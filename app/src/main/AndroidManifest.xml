<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tencent.janimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".pathmeasure.PathMeasureActivity"/>
        <activity android:name=".pathmeasure.PathPaintActivity"/>
        <activity android:name=".pathmeasure.PathTracingActivity"/>
        <activity android:name=".pathmeasure.PathPosTanActivity"/>

        <!--Bezier曲线-->
        <activity android:name=".bezier.BezierActivity"/>
        <activity android:name=".bezier.DrawPadActivity"/>
        <activity android:name=".bezier.PathBezierActivity"/>
        <activity android:name=".bezier.PathMorthingActivity"/>
        <activity android:name=".bezier.SecondBezierActivity"/>
        <activity android:name=".bezier.ThirdBezierActivity"/>
        <activity android:name=".bezier.WaveActivity"/>

        <activity android:name=".vectordrawable.VectorDrawableActivity"/>
        <activity android:name=".vectordrawable.VectorLActivity"/>

    </application>

</manifest>